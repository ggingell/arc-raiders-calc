{
  "name": "arc-raiders-calc",
  "version": "1.0.0",
  "description": "The first (and probably only) calculator available in this project is the k<sub>min</sub> calculatur. It finds the optimal amount of an item to craft in order to use up whole stacks of each input item. This calculator also provides a compression ratio that helps determine whether to craft immediately or only as needed. The decision ultimately depends on the specific item and your particular play style and needs.",
  "main": "main.js",
  "scripts": {
    "dev": "vite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "preview": "vite preview"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
